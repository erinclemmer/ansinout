## ANSINOUT (ANSI + IN + OUT)

Ansinout is a library to help create TUI (Terminal User Interface) programs with Python. 
* Writes ANSI codes to terminal for every character
* Renders can be called continously and only the characters that have changed will be printed

